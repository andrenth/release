(jbuild_version 1)

(library
 ((name release)
  (public_name release)
  (synopsis "blah")
  (libraries
   (stdint lwt lwt.unix lwt_log re.pcre unix))))

(ocamllex (config_lexer))
(ocamlyacc (config_parser))
